<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dai.dao.RegisterMapper">

    <select id="getAllCount" resultType="int">
        SELECT count(userInfo.id)
        FROM userInfo
    </select>

    <select id="getAllAccount" resultType="String">
        SELECT name FROM userInfo
    </select>

    <insert id="insertUserInfo" useGeneratedKeys="true">
        INSERT INTO userInfo (name, password, number, registerTime)
        VALUES (#{name}, #{password}, #{number}, #{registerTime})
    </insert>

    <select id="findPassword" parameterType="String" resultType="String">
        SELECT password FROM userInfo WHERE name = #{name}
    </select>

</mapper>